[project]
name = "data-management"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dataretrieval>=1.3.0",
    "matplotlib>=3.11.1",
    "netcdf4>=1.7.4",
    "pandas>=3.0.5",
    "pyarrow>=25.0.1",
    "xarray>=2026.7.0",
]

[project.scripts]
data-management = "data_management:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
